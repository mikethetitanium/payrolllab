spring.application.name=payroll-reporting-lab

# ============================================
# DATABASE CONFIGURATION
# ============================================
# Uncomment the database you want to use

# --- H2 Database (Default - In-Memory) ---
# spring.datasource.url=jdbc:h2:mem:payrolldb
# spring.datasource.driverClassName=org.h2.Driver
# spring.datasource.username=sa
# spring.datasource.password=
# spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
# spring.h2.console.enabled=true
# spring.h2.console.path=/h2-console

--- SQL Server Configuration ---

# spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=payrolldb;encrypt=true;trustServerCertificate=true
# spring.datasource.driverClassName=com.microsoft.sqlserver.jdbc.SQLServerDriver
# spring.datasource.username=PARAPET
# spring.datasource.password=PARAPET
# spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect

# --- PostgreSQL Configuration ---
# Uncomment below and comment out H2 config above to use PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/payrolldb
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=Mikeville256
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# ============================================
# JPA Configuration
# ============================================
# DDL Strategy:
# - create-drop: DROP tables on shutdown (for testing only)
# - create: CREATE tables on startup (overwrites existing)
# - update: UPDATE schema (keeps existing data) - RECOMMENDED for development
# - validate: Just validate schema (no changes)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ============================================
# Thymeleaf Configuration
# ============================================
spring.thymeleaf.cache=false

# ============================================
# Logging
# ============================================
logging.level.com.payrolllab=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# ============================================
# Spring Boot DevTools Configuration
# ============================================
# Disable auto-restart on file changes
spring.devtools.restart.enabled=false
